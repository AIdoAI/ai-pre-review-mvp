# 同事上手教程：AI 形式化预审 MVP（零基础 → 跑完一个样本）

> 面向首次接手的同事。照本文从头到尾走一遍即可对**新样本文件夹**产出预审报告。
> 配套：`README.md`（设计）、`DEPLOYMENT.md`（环境/编排层细节）。

---

## 0. 这是什么

2026 湖北省国资国企"人工智能+"大赛的**申报材料形式化预审**工具。输入一个样本文件夹（PDF/图片/MinerU JSON），自动判断：**是否合规 / 缺什么 / 需补什么 / 哪些要人工**，产出逐文件报告 + 总览 + 统一结论。

**底线（铁律）**：只有确定性硬规则才判"预审不通过"；解析失败/不全、盖章真伪、语义判断一律**转人工**，绝不误判"材料缺失"。

---

## 1. 一次性环境配置

| 依赖 | 要求 | 命令 |
|---|---|---|
| 代码 | clone 到本地 | `git clone https://github.com/AIdoAI/ai-pre-review-mvp.git ~/ai-pre-review-mvp` |
| Python | **≥ 3.10，用 `python3.13`**（系统默认 python3 常是 3.9，不行） | `python3.13 --version`；缺则 `brew install python@3.13` |
| poppler | 提供 `pdftotext`（本地文字层抽取）/`pdftoppm` | macOS `brew install poppler` |
| MinerU CLI | 文档抽取 | `npm install -g mineru-open-api` |
| MinerU token | OCR/`-f json` 需要（`flash-extract` 免 token） | `mineru-open-api auth`（JWT：https://mineru.net/apiManage/token） |

验证：
```bash
cd ~/ai-pre-review-mvp
python3.13 --version && which pdftotext && mineru-open-api version && mineru-open-api auth --verify
```

> ⚠️ 全程用 **`python3.13`**，别用 `python3`。

---

## 2. 样本文件夹长什么样

一个样本 = 一个文件夹，里面放：
- 申报材料：PDF / 图片（营业执照常是 png）/ 或已抽好的 MinerU JSON。
- **基础资料文件**（可选但强烈建议）：文件名含 `基础资料` / `表单` / `答题`，内含表单选项。例：
  ```
  1、申报方式：联合申报，有联合体协议
  2、是否独立法人：是
  3、项目当前进展：计划实施
  中电云计算技术有限公司
  华中师范大学
  ```

文件夹里**有 MinerU JSON** → 直接审；**只有原始 PDF/图片** → 入口会自动抽取（编排层，见 §3.1）。

---

## 3. 跑一个样本（两种方式）

### 方式 A：交互式入口（人手动跑）
```bash
cd ~/ai-pre-review-mvp
python3.13 run_folder_review.py --input "/绝对路径/某样本文件夹"
```
按提示选申报方式、单位数、牵头方、联合材料三选一、项目阶段。跑完终端打印「统一结论 + 逐文件报告」，文件写在 `output_folder_review/<样本名>/`。

### 方式 B：非交互脚本（推荐给 Claude / 批量）—— 仓库已自带 `run_sample.py`

1）按样本写一个表单 JSON（取值见 `config/form_answers_example.json`），例如 `form.json`：
```json
{
  "mode": "complete",
  "form_answers": {
    "is_joint_declaration": true,
    "joint_declaration_material_type": "stamped_joint_declaration_agreement",
    "project_stage": "planned",
    "applicants": [
      {"entity_id":"E01","entity_name":"中电云计算技术有限公司","entity_type":"state_owned","is_lead":true,"is_independent_legal_person":true},
      {"entity_id":"E02","entity_name":"华中师范大学","entity_type":"other","is_lead":false,"is_independent_legal_person":true}
    ]
  }
}
```
取值说明：
- `is_joint_declaration`：联合 `true` / 独立 `false`
- `joint_declaration_material_type`（联合时三选一）：`stamped_project_cooperation_agreement` / `stamped_joint_declaration_agreement` / `stamped_lead_declaration`
- `project_stage`：`building` 正在建设 / `planned` 计划实施 / `other`
- `entity_type`：`state_owned` / `private` / `other`；`is_lead`/`is_independent_legal_person`：true/false
- `mode`：`complete` 完整材料包（缺件可硬卡）/ `partial` 局部样本（缺件不判缺，默认）

2）跑：
```bash
cd ~/ai-pre-review-mvp
python3.13 run_sample.py --input "/绝对路径/某样本文件夹" --form form.json
```
省略 `--form` 会用脚本顶部的 `DEFAULT_FORM`（独立申报示例）并告警。也可 `--mode` / `--output` / `--submission-id` 覆盖。
脚本自动：扫描 →（无 JSON 时）启动抽取编排层 → 跑审查 → 打印统一结论 + 逐文件报告，文件写在 `output_folder_review/<编号>/`。

### 3.1 抽取编排层（原始文件 → JSON，自动）
没有现成 JSON 时自动分层：
- **Tier0 本地 `pdftotext`**（免网络、毫秒）：有文字层的 PDF 直接用（覆盖率≥60%）。
- **Tier2 MinerU OCR**（含**重试退避 + 大文件分块**）：图片、扫描件、低文字层必传件。
- **Tier3 转人工**：重试仍失败 → `parse_status=failed`，规则层自动转人工。
- **角色预算**：辅助材料（荣誉/研发能力/专利/软著/奖）只本地廉价抽，不烧 OCR。
- 实测：大文件多有文字层走本地；真瓶颈是"小但纯扫描"的必传件（靠重试/分块）。

---

## 4. 产物与怎么读

`output_folder_review/<样本名>/`：

| 文件 | 内容 |
|---|---|
| `conclusion.md` | 统一结论：是否合规 / 缺什么 / 补什么 / 待人工 / 已通过 + 汇总行 |
| `per_file_report.md` | 逐文件「审核项/标准/审核结果」表 + 未提供材料 + 跨文件检查 + 总览表 |
| `review_report.md` | 上两者 + 规则明细全表 |
| `rule_results.json` | 机器可读规则结果（带 HR 编号 + 证据页码） |

状态图标：✅ 合规/已确认　⚠️ 待人工　❌ 不合规　❓ 无法判断　🟡 建议补正。
**综合状态优先级**：不通过 > 待人工 > 建议补正 > 无法判断 > 通过。

---

## 5. 必须知道的规则要点

- **联合申报支持材料 = 三选一**（任一即满足，均须盖章）：项目合作协议 / 联合申报协议 / 牵头方申报声明。选"牵头方申报声明"时还要看"联合申报单位简介"是否补了知识产权无异议表述。
- 7 类必传：申报书、营业执照、信用记录、法定代表人声明函、真实性承诺书、主营业务收入/财务证明、研发投入证明。
- 专利/软著/荣誉 = 辅助材料，**没交不算缺**。
- 盖章真伪、签字真伪、知识产权表述、国资身份 → 一律人工。
- 规则配置在 `config/`（`material_policy.json` / `material_types.json` / `review_rules.json`），组委会可调，**不用改代码**。

---

## 6. 常见坑

| 现象 | 原因 / 处理 |
|---|---|
| `SyntaxError` / 版本报错 | 用了 `python3`（3.9）。改用 `python3.13`。 |
| `pdftotext: command not found` | `brew install poppler` |
| OCR `context deadline exceeded` | 偶发网络/`--ocr` 负载。编排层已自动重试+分块；仍失败的文件会标"待人工"，不影响其它。 |
| 营业执照 png 没文字 | 编排层会对图片走 OCR；若 OCR 失败 → 转人工，不判不合规。 |
| 跑测试报 3 失败 | 仓库未附样例 `input/*.json`，属预期；核心测试应全绿。测试需 `ln -s ~/ai-pre-review-mvp ~/local_review`。 |

---

## 7.（关键）给"无 context 的 Claude Code"的提示词 —— 复制即用

> 在样本所在机器上打开 Claude Code，把下面整段贴进去，并把【】替换为实际值。

```
你在帮我跑"2026 湖北省国资国企 AI+大赛"申报材料的形式化预审。请严格按步骤执行，全程用 python3.13。

仓库：~/ai-pre-review-mvp（不存在就 git clone https://github.com/AIdoAI/ai-pre-review-mvp.git ~/ai-pre-review-mvp，存在就 git pull）。
样本文件夹（绝对路径）：【/Users/xxx/.../某样本】

【第一步 环境自检】依次检查并补齐：python3.13 --version（缺则 brew install python@3.13）；which pdftotext（缺则 brew install poppler）；mineru-open-api version（缺则 npm install -g mineru-open-api）；mineru-open-api auth --verify（失败则提示我先跑 mineru-open-api auth）。任一缺失且无法自动补齐，先停下报告，别绕过。

【第二步 读表单】在样本文件夹找 *基础资料* / *表单* / *答题* 文件并读全文，抽取：申报方式(独立/联合)、联合单位数、牵头单位、联合材料三选一(合作协议/联合申报协议/牵头方申报声明)、各单位名称、是否独立法人、项目当前进展(正在建设/计划实施)。把推断结果列给我确认一次，我确认后再继续。单位性质(国企/民企)不要采纳基础资料里的说法，以营业执照/股权材料为准或转人工。

【第三步 跑审查（非交互）】不要用交互式 run_folder_review.py 的菜单。改为写一段 python3.13 脚本：用 review_mvp.folder_review.scan_sample_folder 扫描；若无 MinerU JSON 则用 review_mvp.extract_orchestrator.orchestrate_folder 从原始文件抽取（本地文字层→OCR重试/分块→转人工，缓存写 output_folder_review/_mineru_cache/）；构造 manifest（submission 含 mode、form_answers=上一步表单、files=抽取结果），调用 review_mvp.pipeline.run_manifest 跑完。mode 用 complete（完整包）或 partial（局部样本），按材料是否齐全判断，拿不准用 partial。

【第四步 输出】在对话里完整打印 run_manifest 结果里的 conclusion 和 per_file_report（不是摘要），并告诉我 review_report.md 路径。

【铁律，必须遵守】
- 只有 Python 规则引擎的硬规则能判"预审不通过"，你不能自己判不通过。
- 未识别≠缺失；解析失败/不全→转人工；OCR/读取失败→转人工，绝不判不合规。
- 盖章真伪、签字真伪、知识产权表述、国资身份→转人工。
- 专利/软著/荣誉等辅助材料没交，不算材料缺失。
- 每条结论尽量带原文件+页码证据。
- 不要把真实测试材料 push 到 git；不要覆盖我已有的输出目录。
```

---

## 8. 一句话总结给同事

`clone → 配 python3.13/poppler/mineru token → 把样本文件夹丢给方式 B 脚本（或第 7 节提示词给 Claude）→ 读 output_folder_review/<样本>/review_report.md`。看不懂的判定看图标和"待人工处理项"，硬规则不通过才需要退件，其余按报告补正或人工复核。
