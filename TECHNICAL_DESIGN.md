# 本地AI预审MVP技术设计

## 目标

利用现有 MinerU JSON 验证AI预审流程，暂不依赖腾讯云函数或正式平台。

## 核心原则

1. MinerU负责OCR、表格和版面读取，不负责审查结论。
2. 解析失败不等于材料缺失。
3. 局部测试样本中未出现的材料标记为“无法判断”。
4. 只有完整材料包且解析成功后，才能对缺失材料自动判定不通过。
5. 所有结果必须保留证据文件和页码。
6. 公章、签字、证书真伪及复杂语义进入人工复核。
7. 专利、软著、奖项等鼓励材料未提交不得打回；无关材料仅提示，不参与硬规则。
8. 不采用“首次失败立即短路”；应完成全部可执行检查，一次性返回问题清单。
9. 必要材料的“分类结果”和“存在性确认”分开：弱关键词只能形成疑似材料，不能让硬规则通过。

## 模块

| 模块 | 文件 | 职责 |
|---|---|---|
| MinerU读取 | `review_mvp/mineru_reader.py` | 读取文本、表格、图片和页码 |
| 材料分类 | `review_mvp/classifier.py` | 识别材料类型并合并跨页内容 |
| 字段抽取 | `review_mvp/extractor.py` | 抽取企业名称、信用代码、联系人等 |
| 规则引擎 | `review_mvp/rule_engine.py` | 执行存在性和确定性规则 |
| 主体结构 | `review_mvp/subject_structure.py` | 校验独立/联合申报、多主体、独立法人、上级单位和材料归属 |
| 报告生成 | `review_mvp/report.py` | 输出Markdown人工审查报告 |
| 资质画像 | `review_mvp/qualification_profile.py` | 仅根据文件名分类、去重、统计资质成果候选 |
| 批量入口 | `run_review.py` | 批量运行样本清单 |
| 交互测试入口 | `run_folder_review.py` | 扫描单个样本文件夹，运行时收集表单选项并自动生成清单 |
| 资质画像入口 | `run_qualification_profile.py` | 生成专家用资质画像JSON与Markdown |
| 能力声明 | `config/rule_capabilities.json` | 声明每条业务规则当前是否可实现及能否自动拒绝 |

## 资质画像顺序

资质画像与必要材料审查分开执行。系统可以并行生成画像，但只有通过形式审查后才向专家展示。专利、软著、论文、奖项等候选项不参与缺件打回；文件名初步分类也不能证明权属、有效性或含金量。

## 主体结构与动态材料

`subject_structure`将申报方式和主体关系列为结构化输入。联合申报单位只统计`lead/member`角色；仅提供授权的上级单位使用`authorizing_parent`，不计入联合体数量。非独立法人申报单位必须绑定上级独立法人，并分别关联上级营业执照和专项授权文件。

`material_assignments`用于声明材料归属。系统识别到同类材料但无法确认属于哪家单位时，只转人工复核，不直接认为对应主体已提交。

正式平台对接优先传入`form_answers`。是否联合申报、是否独立法人和牵头单位均由用户手动选择；适配层据此生成内部`subject_structure`、兼容条件标签和动态上传区域。上传区域应随文件携带`owner_entity_id`与`supports_entity_id`，管线会自动建立材料归属。

项目阶段允许表单选择`building`、`planned`或`other`；`other`当前只触发人工复核。联合申报支持材料通过`joint_declaration_material_type`记录“盖章项目合作协议 / 盖章联合申报协议 / 盖章的牵头方申报声明”三选一，当前仅生成对应上传区域，存在性、盖章和内容完整性留待后续解析能力或人工复核。

## 长文件处理

超长财务、信用中国等材料不建议全文深度理解：

1. 保留原始文件和总页数。
2. 按30至50页拆分解析，并保存原页码映射。
3. 优先定位目录、利润表、营业收入和研发费用相关页。
4. 未解析页标记为`partial`，相关规则转人工复核。

解析状态：

- `success`：可参与材料存在性判断。
- `partial`：仅对已识别内容给出结果，未发现的材料不能判缺失。
- `failed`：解析失败，转人工。
- `pending`：仍在等待解析，不执行最终判断。

## 后续接入正式平台

本地流程验证稳定后，可以整体封装为HTTP API：

```text
平台上传文件
-> MinerU API
-> 本地MVP逻辑
-> 返回材料目录、字段、规则结果和证据页码
```

无需改变材料Schema和规则输出结构。
