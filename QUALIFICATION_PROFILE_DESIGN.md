# 文件名资质画像模块

## 用途

本模块在不读取材料正文的前提下，根据文件名和目录结构，形成供专家参考的企业资质与成果候选画像。

它与形式审查相互独立：

1. 形式审查先判断必要材料是否齐全。
2. 资质画像可以并行生成，但不参与缺件打回。
3. 通过形式审查后，再向专家展示资质画像摘要和候选明细。

## 输出

- 原始文件数和去重后候选数。
- 专利、软著、论文、奖项、获批平台、认证等分类统计。
- AI相关成果候选数量。
- 2至3句专家摘要。
- 每一项的文件名、分类依据、置信度和重复情况。

所有结果统一标记为`filename_inferred`。文件名不能证明权属、有效性、证书真伪或含金量，相关结论必须结合正文或人工核验。

## 运行

```bash
python3 run_qualification_profile.py \
  --input "/path/to/企业资质文件夹" \
  --output output_qualification/企业名称 \
  --entity-name "企业名称"
```

生成：

- `qualification_profile.json`：供系统或后续大模型使用的结构化结果。
- `qualification_profile.md`：供专家快速浏览的摘要和明细。

如果一个总目录下按企业分文件夹存放，可按一级目录批量统计：

```bash
python3 run_qualification_profile.py \
  --input "/path/to/全部企业资质" \
  --output output_qualification/batch \
  --group-by-first-level
```

总目录下无法归属到企业子文件夹的文件会单独进入`未归属文件`分组，避免静默遗漏。

## 后续增强

文件名MVP稳定后，可只对低置信度候选调用MinerU或大模型：

- 确认`电子证书`究竟是专利、软著还是其他证书。
- 抽取证书编号、权利人、有效期和授权状态。
- 对专利类型、奖项级别、成果与参赛项目相关性做分层。

这种按需解析方式可以显著减少长文档排队和模型调用成本。
