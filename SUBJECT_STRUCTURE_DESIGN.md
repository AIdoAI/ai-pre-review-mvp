# 申报主体结构与动态材料规则

## 判断顺序

```text
申报项目
-> 判断独立申报或联合申报
-> 建立实际申报单位清单
-> 确定牵头单位和联合成员
-> 逐家判断是否独立法人
-> 非独立法人绑定上级独立法人单位
-> 根据主体属性生成动态材料要求
-> 将识别材料关联至对应主体
-> 分主体输出缺件与人工复核事项
```

## 主体角色

- `applicant`：独立申报单位。
- `lead`：联合申报牵头单位。
- `member`：联合申报成员。
- `authorizing_parent`：仅为非独立法人提供营业执照和授权的上级单位，不计入联合申报单位数量。

联合申报单位数量仅统计`lead`和`member`，必须为2至3家，并且只能有1家牵头单位。

## 非独立法人分支

凡`is_independent_legal_person=false`的申报单位，不依赖“子公司、分公司、分院”等名称关键词，统一触发：

1. 绑定`parent_entity_id`指向具有独立法人资格的上级单位。
2. 检查上级单位营业执照。
3. 检查上级单位出具的专项授权申报证明文件。

上级单位仅提供授权时，不属于联合申报成员。

## 材料归属

`material_assignments`通过`owner_entity_id`声明材料属于谁，通过`supports_entity_id`声明授权文件支持哪家非独立法人申报单位。

系统识别到同类材料但无法确认归属时，转人工复核；只有完整材料包、解析完整且完全未识别到所需材料时，才允许判缺。

每家主体还可配置`required_materials`。该字段应由组委会确认后填写，系统不会自行推导未明确的联合成员材料要求。

完整JSON示例见`config/subject_structure_example.json`。
