# 给同事的提示词（复制即用 · Windows/macOS 通用）

**同事用法**：打开 Claude Code（在终端 / PowerShell 运行 `claude`），把下面整段贴进去，
**只改第一行的样本文件夹路径**，回车。提示词会让 Claude 自己判断系统、找 Python、确认依赖。

> **唯一需要你手动做一次的**：在终端运行 `mineru-open-api auth` 配置 MinerU token（交互式登录，
> Claude 代替不了）。JWT 见 https://mineru.net/apiManage/token 。其余（Python、poppler、Node/MinerU CLI、
> 克隆代码）提示词会让 Claude 自检并安装。
>
> Windows 路径示例：`C:\Users\你的名字\Downloads\参赛用户31`（含中文/空格，务必整体加引号）。

---

```
你是我的助手，帮我跑"2026 湖北省国资国企人工智能+大赛"申报材料的形式化预审。请按步骤执行；遇到装不了/确认不了的，先停下告诉我，不要绕过。

【样本文件夹（绝对路径）】：<<在这里粘贴文件夹路径，Windows 如 C:\Users\名字\Downloads\参赛用户31；mac 如 /Users/名字/Downloads/参赛用户31>>

第1步 判断系统与 Python（关键，先做）：
- 先判断我的操作系统（Windows 还是 macOS）。
- 找一个可用的 Python（版本 ≥ 3.10）：依次尝试 `py -3 --version`、`python --version`、`python3.13 --version`，把第一个能用且 ≥3.10 的命令记作 PYTHON，本次之后所有 python 命令都用 PYTHON。
  - 都没有：Windows 执行 `winget install Python.Python.3.12`（提示我在安装界面勾选 Add to PATH），装完让我重开一个终端再继续；macOS 执行 `brew install python@3.13`。

第2步 确认其余依赖（Windows 用 `where.exe 命令名` 检查，macOS 用 `which 命令名`）：
- poppler 的 `pdftotext`：缺 → Windows `choco install poppler`，没有 choco 就 `scoop install poppler`，再没有就让我手动装 poppler-windows 并把其 bin 目录加入 PATH；macOS `brew install poppler`。
- `node`/`npm` 与 `mineru-open-api`：缺 node → Windows `winget install OpenJS.NodeJS.LTS`（装完重开终端）、macOS 用 brew 或自带；然后 `npm install -g mineru-open-api`。
- `mineru-open-api auth --verify`：若失败，告诉我在终端运行 `mineru-open-api auth` 配 token，然后停下等我。

第3步 取代码：仓库目录若不存在，`git clone https://github.com/AIdoAI/ai-pre-review-mvp.git`（克隆到我的用户主目录下，并记住克隆后的完整路径）；已存在则进入该目录 `git pull`。

第4步 读表单：在样本文件夹里找名字含"基础资料/表单/答题"的文件读全文，推断：申报方式(独立/联合)、联合单位数与各单位名称、牵头单位、联合材料三选一(合作协议/联合申报协议/牵头方申报声明)、是否独立法人、项目当前进展(正在建设/计划实施)。把推断结果列出来让我确认一次；我确认后在样本文件夹里写 form.json：
{"mode":"complete 或 partial","form_answers":{"is_joint_declaration":true,"joint_declaration_material_type":"stamped_project_cooperation_agreement | stamped_joint_declaration_agreement | stamped_lead_declaration | null","project_stage":"building | planned | other","applicants":[{"entity_id":"E01","entity_name":"…","entity_type":"state_owned | private | other","is_lead":true,"is_independent_legal_person":true}]}}
材料齐全用 complete，只是局部样本用 partial。单位性质不要只信基础资料，以营业执照/股权为准，拿不准就让相应检查转人工。

第5步 跑审查（在仓库目录下，PYTHON 用第1步找到的命令；路径含中文/空格要加引号）：
PYTHON run_sample.py --input "<<样本文件夹路径>>" --form "<<样本文件夹路径>>/form.json"
脚本会自动：没有 MinerU JSON 时先分层抽取（本地文字层→OCR重试/分块→转人工），再跑规则。大文件/扫描件可能慢，请耐心等；某个文件 OCR 失败会自动标"待人工"，不影响其它文件。

第6步 输出：把脚本打印的"统一结论"和"逐文件报告"完整发给我（不要只给摘要），并告诉我 review_report.md 的完整路径。

铁律（必须遵守）：
- 只有脚本里的硬规则能判"预审不通过"，你自己不能下"不通过"结论。
- 没识别到 ≠ 缺失；解析失败/不全、OCR失败 → 转人工，绝不判不合规。
- 盖章真伪、签字真伪、知识产权表述、国资身份 → 转人工。
- 专利/软著/荣誉等辅助材料没交，不算缺。
- 不要把样本材料 push 到 git，不要覆盖我已有的输出目录。
```

---

## 一次跑多个样本（批量）

把上面【样本文件夹】改成一个**父目录**，并补一句给 Claude：

```
这是一个父目录，下面每个子文件夹是一个样本。请给每个子文件夹分别读基础资料、生成各自的 form.json（放进对应子文件夹），然后在仓库目录下用（PYTHON 为第1步找到的命令）：
PYTHON run_sample.py --input "<<父目录路径>>" --parent
跑完把 output_folder_review/batch_summary.md（含「牵头单位/申报主体」列）完整发给我，并逐个样本给出结论。
```

要点：批量时每个样本文件夹各放一个 `form.json`（脚本优先读样本内的表单）；批量结果汇总在
`output_folder_review/batch_summary.md`。
