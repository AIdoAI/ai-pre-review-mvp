# 给预审同事的提示词（复制即用 · 非技术也能跑）

预审实际操作：把下面提示词整段贴进 Claude Code（终端 / PowerShell 里运行 `claude` 打开），
按提示替换【】里的内容，回车。Claude 会自己装环境、读 Excel、抽材料、出预审结论。

> **你只需要准备两样：**
> 1. 一个**批次父目录**：里面有一个**明细 Excel** + 若干 `参赛用户N` 子文件夹（每个子文件夹放该参赛单位的材料）。
> 2. Excel 的**打开密码**（如果加密了的话）。
>
> **首次**还需在终端自己跑一次 `mineru-open-api auth` 配 MinerU token（登录拿密钥，Claude 替不了）。
> Token 申请：https://mineru.net/apiManage/token 。

---

## 主推：整批预审（Excel 驱动，一次跑完一批）

```
你是预审助手，帮我对一个批次的参赛材料做形式预审。按步骤做，遇到装不上/读不了的先停下告诉我，别硬绕。命令里的 python3.13 在 Windows 上请换成 py -3 或 python。

【批次父目录（绝对路径）】：<<父目录，里面有 1 个明细 Excel + 若干 参赛用户N 子文件夹。Windows 形如 C:\Users\名字\Downloads\某批次>>
【Excel 打开密码（没加密就填：无）】：<<如 8fai123>>

第1步 备好环境（缺啥装啥，装不了就停下告诉我）：
- 代码仓库 ~/ai-pre-review-mvp：不存在就 git clone https://github.com/AIdoAI/ai-pre-review-mvp.git ~/ai-pre-review-mvp ；存在就进去 git pull。
- 找可用 python(≥3.10)：依次试 py -3 --version / python --version / python3.13 --version，记住能用的那个当作 PYTHON。缺就装（Windows: winget install Python.Python.3.12 勾 Add to PATH；mac: brew install python@3.13）。
- 确认 pdftotext、mineru-open-api 在 PATH（Windows 用 where.exe，mac 用 which）。缺 pdftotext → 装 poppler（Windows: choco/scoop install poppler；mac: brew install poppler）。缺 mineru-open-api → 先装 node（Windows: winget install OpenJS.NodeJS.LTS；mac 用 brew）再 npm install -g mineru-open-api。
- 装读 Excel 的库：PYTHON -m pip install openpyxl msoffcrypto-tool（报 externally-managed 就加 --break-system-packages）。
- PYTHON -m? 不行就直接 pip install。最后 mineru-open-api auth --verify，失败就让我先在终端跑 mineru-open-api auth，然后停下等我。

第2步 跑整批预审（自动找目录里的明细 Excel，按它给每家生成表单）：
- 先在父目录里找到那个明细 .xlsx 文件名。
- 然后执行（PYTHON 用第1步找到的；路径含中文/空格要加引号）：
  cd ~/ai-pre-review-mvp && PYTHON run_sample.py --input "<<父目录>>" --parent --excel "<<父目录>>/<<明细Excel文件名>>" --excel-password "<<密码>>"
- 扫描件多的会比较慢（可能几分钟到十几分钟），耐心等；个别文件 OCR 失败会自动标"待人工"，不影响其它文件。

第3步 输出结果：
- 脚本跑完会在终端"统一打印"：批次总表 + 每个参赛单位的【统一结论 + 逐文件表】。把这些终端输出**原样完整发我**（不要改写/截断），每家后面可再补一两句大白话提醒人工注意点。
- 这些内容同时已写入文件：~/ai-pre-review-mvp/output_folder_review/<单位文件夹名>/{conclusion.md, per_file_report.md, review_report.md} 和批次 batch_summary.md。确认文件已落盘，并把每家 review_report.md 的完整绝对路径列给我。

底线（务必遵守）：
- 只有程序的硬规则能判"预审不通过"，你自己不能下"不通过"结论。
- 没识别到 ≠ 材料缺失；解析失败/不全、OCR失败 → 转人工，绝不判不合规。
- 盖章真伪、签字真伪、知识产权表述、单位是不是国企 → 都转人工。
- 专利/软著/荣誉等辅助材料没交，不算缺。
- 不要把参赛材料 push 到 git，不要覆盖我已有的输出目录。
```

---

## 备用：只跑单个参赛单位（没有 Excel、手工给表单）

```
你是预审助手，帮我对一个参赛单位做形式预审。按步骤做，缺东西先停下告诉我。python3.13 在 Windows 上换成 py -3 或 python。

【参赛单位文件夹（绝对路径）】：<<该单位材料所在文件夹>>

1. 环境同上（仓库 git pull；python≥3.10；pdftotext；mineru-open-api auth --verify）。
2. 在文件夹里找"基础资料/表单"文件读全文，推断：申报方式(独立/联合)、联合单位与牵头、联合材料三选一(合作协议/联合申报协议/牵头方申报声明)、是否独立法人、项目进展(正在建设/计划实施)。列给我确认一次，我确认后写成 form.json 放进该文件夹：
{"mode":"complete 或 partial","form_answers":{"is_joint_declaration":true,"joint_declaration_material_type":"stamped_project_cooperation_agreement | stamped_joint_declaration_agreement | stamped_lead_declaration | null","project_stage":"building | planned | other","applicants":[{"entity_id":"E01","entity_name":"…","is_lead":true,"is_independent_legal_person":true}]}}
（单位性质别只信表单，以营业执照为准，拿不准就转人工。联合申报至少 2 家。）
3. cd ~/ai-pre-review-mvp && python3.13 run_sample.py --input "<<文件夹>>" --form "<<文件夹>>/form.json"
4. 输出（两件都做）：把 ~/ai-pre-review-mvp/output_folder_review/<单位文件夹名>/conclusion.md 和 per_file_report.md 的内容"原样完整"贴进对话（统一格式，别改写/别截断），再补一两句大白话；并确认文件已写盘、给出 review_report.md 完整绝对路径。
5. 底线同上：只有程序硬规则能判不通过；解析失败/盖章/国资身份转人工；不 push 材料、不覆盖输出。
```
