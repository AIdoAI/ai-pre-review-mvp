# 给预审同事的提示词（复制即用）

**怎么用：** 打开 Claude Code（终端 / PowerShell 里输入 `claude`），把下面【主推】整段**原样复制**进去，
**只改第一处"批次文件夹"那一行**（换成你电脑上该批次文件夹的完整路径），回车。剩下 Claude 全自动。

> **你只需要 1 样东西：** 你负责的那个**批次文件夹**的完整路径（里面有 5 个 `参赛用户N` + 1 个加密 Excel）。
> Excel 密码固定 **8fai123**，已经写进提示词，不用动。
>
> **首次**先在终端单独跑一次 `mineru-open-api auth` 配 MinerU 密钥（登录拿 key，Claude 替不了）。
> 申请地址：https://mineru.net/apiManage/token 。

> **⚠️ 之前用旧版报过错（中文 emoji 崩 / Excel 匹配到 0 家走默认表单）的同事，先做 3 步再用：**
> 1. **更新代码**：`cd ~/ai-pre-review-mvp && git pull`
> 2. **撤销手改**：之前手动改过的 stdout 编码、anaconda 的 termios.py 等，全部还原/删掉（不再需要）。
> 3. **重跑**：用下面提示词重跑，Excel 不用换，还是原来那个加密表。

---

## 主推：整批预审（只改 1 行，照贴）

```
你是预审助手。我给你一个"批次文件夹"，里面有最多 5 个参赛用户的材料 + 一个加密 Excel 明细表。
请对这些参赛用户做形式预审，最后按统一格式把结果给我。按步骤做，遇到装不上/读不了就停下问我，别硬撑。

==== 我只改这一行（换成我的批次文件夹完整路径）====
批次文件夹：C:\Users\你的电脑用户名\Downloads\湖北国资国企AI+大赛应用场景申报\李璐洋_226-273\批次07_参赛用户256-260

（Excel 打开密码固定 8fai123；批次号和参赛用户编号你从文件夹名自己认出来，先回我"本批要审哪几个用户"让我确认一下。）

第1步 备环境（命令里 PYTHON：Windows 用 py -3 或 python，Mac 用 python3.13。缺什么装什么，装不了就停下告诉我）：
- 代码仓库 ~/ai-pre-review-mvp：有就 git pull；没有就 git clone https://github.com/AIdoAI/ai-pre-review-mvp.git ~/ai-pre-review-mvp
- 确保这几样能用：python(≥3.10)、poppler 的 pdftotext、mineru-open-api。再 PYTHON -m pip install openpyxl msoffcrypto-tool（报 externally-managed 就加 --break-system-packages）。
  · Windows 装法：python→winget install Python.Python.3.12（勾 Add to PATH）；pdftotext→choco install poppler 或 scoop install poppler；mineru→winget install OpenJS.NodeJS.LTS 后 npm install -g mineru-open-api
- 最后 mineru-open-api auth --verify；不过就让我先在终端跑 mineru-open-api auth，然后停下等我。

第2步 跑预审（把命令里 3 处 <批次文件夹> 都替换成上面那行的路径；中文/空格路径要带引号）：
cd ~/ai-pre-review-mvp ; PYTHON run_sample.py --input "<批次文件夹>" --parent --excel "<批次文件夹>\湖北国资国企AI+大赛应用场景申报_联系人电话脱敏.xlsx" --excel-password 8fai123 --output "<批次文件夹>\_预审结果"
（扫描件多会慢，几分钟到十几分钟，耐心等；个别文件 OCR 失败会自动标"待人工"，不影响别的。）

第3步 把结果给我：
- 脚本跑完会在终端统一打印：批次总表 + 每个参赛用户的【结论 + 逐文件表】。把这些原样完整发我（不要改写/截断），每家后面加一两句大白话提醒我重点看什么。
- 结果也已存到 <批次文件夹>\_预审结果\ 里；把每家 review_report.md 的完整路径列给我。

铁律（必须遵守）：
- 只有程序的硬规则能判"预审不通过"，你不能自己下"不通过"。
- 没读到 ≠ 材料缺失；读不了 / OCR 失败 → 转人工，绝不判不合规。
- 盖章真伪、签字、知识产权表述、单位是不是国企 → 都转人工。
- 专利 / 软著 / 荣誉等辅助材料没交，不算缺。
- 不要把参赛材料 push 到 git，不要覆盖我已有的结果。
```

> 路径示例里的 `你的电脑用户名` 换成你的 Windows 账户名；最稳的办法：在资源管理器里进到该批次文件夹，
> 点地址栏复制完整路径，直接粘到"批次文件夹"那行。

---

## 备用：只跑单个参赛单位（手里没 Excel 时）

```
你是预审助手，帮我对一个参赛单位做形式预审。按步骤做，缺东西先停下告诉我。PYTHON：Windows 用 py -3 或 python，Mac 用 python3.13。

参赛单位文件夹：<<换成该单位材料文件夹的完整路径>>

1. 环境同上（仓库 git pull；python≥3.10；pdftotext；mineru-open-api auth --verify）。
2. 在文件夹里找"基础资料/表单"文件读全文，推断：申报方式(独立/联合)、联合单位与牵头、联合材料三选一(盖章项目合作协议/盖章联合申报协议/盖章的牵头方申报声明)、是否独立法人、项目进展(正在建设/计划实施)。列给我确认一次，确认后写成 form.json 放进该文件夹：
{"mode":"complete 或 partial","form_answers":{"is_joint_declaration":true,"joint_declaration_material_type":"stamped_project_cooperation_agreement | stamped_joint_declaration_agreement | stamped_lead_declaration | null","project_stage":"building | planned | other","applicants":[{"entity_id":"E01","entity_name":"…","is_lead":true,"is_independent_legal_person":true}]}}
（单位性质别只信表单，以营业执照为准，拿不准就转人工。联合申报至少 2 家。选"牵头方申报声明"时，提醒人工检查"联合申报单位简介"是否补了知识产权无异议。）
3. cd ~/ai-pre-review-mvp ; PYTHON run_sample.py --input "<单位文件夹>" --form "<单位文件夹>/form.json"
4. 把 conclusion.md 和 per_file_report.md 原样完整贴进对话，再补一两句大白话；给出 review_report.md 完整路径。
5. 铁律同主推：只有程序硬规则能判不通过；解析失败/盖章/国资身份转人工；不 push 材料、不覆盖输出。
```
