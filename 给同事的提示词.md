# 给同事的提示词（复制即用）

**同事用法**：在装好环境的电脑上打开 Claude Code（终端运行 `claude`），把下面整段贴进去，
**只改第一行的样本文件夹路径**，回车即可。

> 首次使用前，同事需在终端自己跑一次 `mineru-open-api auth` 配置 MinerU token（交互式登录，
> Claude 代替不了）。JWT 见 https://mineru.net/apiManage/token 。其余环境（python3.13、poppler、
> mineru CLI）提示词里会让 Claude 自检并安装。

---

```
你是我的助手，帮我跑"2026 湖北省国资国企人工智能+大赛"申报材料的形式化预审。严格按步骤执行，全程用 python3.13（不要用 python3）。

【样本文件夹（绝对路径）】：<<在这里粘贴文件夹路径，例如 /Users/你的用户名/Downloads/参赛用户31>>

第1步 准备代码与环境（缺啥装啥；装不了就停下告诉我，别绕过）：
- 仓库 ~/ai-pre-review-mvp：不存在则 git clone https://github.com/AIdoAI/ai-pre-review-mvp.git ~/ai-pre-review-mvp ；存在则 cd ~/ai-pre-review-mvp && git pull
- 逐项检查：python3.13 --version（缺→brew install python@3.13）；which pdftotext（缺→brew install poppler）；mineru-open-api version（缺→npm install -g mineru-open-api）；mineru-open-api auth --verify（失败→告诉我去终端运行 mineru-open-api auth 配 token，然后停下等我）。

第2步 读表单：在样本文件夹里找名字含"基础资料/表单/答题"的文件并读全文，推断：申报方式(独立/联合)、联合单位数与各单位名称、牵头单位、联合材料三选一(合作协议/联合申报协议/牵头方申报声明)、是否独立法人、项目当前进展(正在建设/计划实施)。把推断结果列出来让我确认一次；我确认后写成 form.json：
{"mode":"complete 或 partial","form_answers":{"is_joint_declaration":true,"joint_declaration_material_type":"stamped_project_cooperation_agreement | stamped_joint_declaration_agreement | stamped_lead_declaration | null","project_stage":"building | planned | other","applicants":[{"entity_id":"E01","entity_name":"…","entity_type":"state_owned | private | other","is_lead":true,"is_independent_legal_person":true}]}}
材料齐全用 complete，只是局部样本用 partial。单位性质不要只信基础资料，以营业执照/股权为准，拿不准就让相应检查转人工。

第3步 跑审查：
cd ~/ai-pre-review-mvp && python3.13 run_sample.py --input "<<样本文件夹路径>>" --form form.json
脚本会自动：没有 MinerU JSON 时先分层抽取（本地文字层→OCR重试/分块→转人工），再跑规则。大文件/扫描件可能慢，请耐心等；某个文件 OCR 失败会自动标"待人工"，不影响其它文件。

第4步 输出：把脚本打印的"统一结论"和"逐文件报告"完整发给我（不要只给摘要），并告诉我 review_report.md 的路径。

铁律（必须遵守）：
- 只有脚本里的硬规则能判"预审不通过"，你自己不能下"不通过"结论。
- 没识别到 ≠ 缺失；解析失败/不全、OCR失败 → 转人工，绝不判不合规。
- 盖章真伪、签字真伪、知识产权表述、国资身份 → 转人工。
- 专利/软著/荣誉等辅助材料没交，不算缺。
- 不要把样本材料 push 到 git，不要覆盖我已有的输出目录。
```

---

## 一次跑多个样本（批量）

把上面提示词的【样本文件夹】改成一个**父目录**，并在第 3 步让 Claude 用批量命令即可。也可以直接补一句给 Claude：

```
这是一个父目录，下面每个子文件夹是一个样本。请给每个子文件夹分别读基础资料、生成各自的 form.json（放进对应子文件夹），然后用：
cd ~/ai-pre-review-mvp && python3.13 run_sample.py --input "<<父目录路径>>" --parent
跑完把 output_folder_review/batch_summary.md（含「牵头单位/申报主体」列）完整发给我，并逐个样本给出结论。
```

要点：批量时每个样本文件夹各放一个 `form.json`（脚本优先读样本内的表单），批量结果汇总在
`output_folder_review/batch_summary.md`。
